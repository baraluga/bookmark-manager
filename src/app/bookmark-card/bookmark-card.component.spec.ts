import { BookmarkCardComponent } from './bookmark-card.component';

describe('BookmarkCardComponent', () => {
  let component: BookmarkCardComponent;

  beforeEach(() => {
    component = new BookmarkCardComponent();
  });

  it('should be OK!', () => {
    expect(component).toBeTruthy();
  });
});
